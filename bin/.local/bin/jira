function jira() {
    arg="$1"
    url="https://jira.cloudera.com/browse/"
    if echo ${arg} | grep -q "^[0-9]\+" ; then
        ticket="DSE-"${arg}
    else
        ticket=${arg}
    fi
    echo $url$ticket
    open "$url$ticket"
}

jira $1
